{
    "name": "proma",
    "version": "0.0.1",
    "author": {
        "name": "Simon Schödler"
    },
    "main": "build/index.js",
    "scripts": {
        "compile": "tsc && copyfiles -f src/index.html build",
        "start": "npm run compile && electron ."
    },
    "dependencies": {
        "nedb": "^1.8.0"
    },
    "devDependencies": {
        "copyfiles": "^2.4.0",
        "electron": "^10.1.3",
        "typescript": "^4.0.3"
    }
}
